require "hashdiff"

module Hashdiff
  # def diff(obj1 : K8S::Internals::HashObject, obj2 : K8S::Internals::HashObject, **options)
  #   diff(obj1.to_h, obj2.to_h, **options)
  # end

  # private def any_hash_or_array?(obja, objb)
  #   obja.is_a?(Array) || obja.is_a?(Hash) || obja.is_a?(Enumerable) || objb.is_a?(Array) || objb.is_a?(Hash) || objb.is_a?(Enumerable)
  # end
end
