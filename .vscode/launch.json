{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Compile",
      "type": "lldb",
      "program": "${workspaceFolder}/bin/test",
      "args": ""
    }
  ]
}
